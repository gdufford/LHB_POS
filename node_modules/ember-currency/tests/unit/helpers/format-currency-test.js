import {
  formatCurrency
} from 'ember-currency/helpers/format-currency';

module('FormatCurrencyHelper');

test('it correctly renders a currency', function() {
  var result = formatCurrency('1234.56789');
  equal(result.toString().trim(), '1,234.56');
});
